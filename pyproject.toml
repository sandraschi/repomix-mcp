[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "repomix-mcp"
version = "0.1.0"
description = "MCP server for packing repositories into AI-friendly formats using Repomix"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.12"
authors = [
    {name = "FlowEngineer sandraschi", email = "sandraschi@example.com"},
]
maintainers = [
    {name = "FlowEngineer sandraschi", email = "sandraschi@example.com"},
]
keywords = [
    "mcp",
    "model-context-protocol",
    "repomix",
    "repository",
    "analysis",
    "ai",
    "codebase",
    "compression",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Utilities",
]
dependencies = [
    "fastmcp>=3.4.4,<4",
    "pydantic>=2.0.0",
    "pathlib>=1.0.0",
    "ruff>=0.16.0,<0.17",
    "prefab-ui>=0.14.0",
    "fastapi>=0.115",
    "uvicorn[standard]>=0.34",
]

[project.urls]
Homepage = "https://github.com/mlopezgez/mcp-server-repomix"
Repository = "https://github.com/mlopezgez/mcp-server-repomix"
Issues = "https://github.com/mlopezgez/mcp-server-repomix/issues"
Changelog = "https://github.com/mlopezgez/mcp-server-repomix/blob/main/CHANGELOG.md"

[project.scripts]
repomix-mcp = "repomix_mcp.server:main"

[tool.setuptools]
packages = ["repomix_mcp"]
package-dir = {"" = "src"}

[tool.setuptools.package-data]
repomix_mcp = ["*.txt", "*.md"]

[tool.mypy]
python_version = "3.9"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
disallow_incomplete_defs = true
check_untyped_defs = true
disallow_untyped_decorators = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true
warn_no_return = true
warn_unreachable = true
strict_equality = true

[[tool.mypy.overrides]]
module = [
    "fastmcp.*",
]
ignore_missing_imports = true

[tool.ruff]
line-length = 120
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "S", "UP", "RUF"]
ignore = ["S101", "B008", "S106", "S110", "S112", "S101", "B008", "S106", "S110", "S112", "RUF012", "S603", "S607", "S324", "E402", "E501"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[project.optional-dependencies]
dev = ["pytest>=8.0", "ruff>=0.16.0,<0.17"]
